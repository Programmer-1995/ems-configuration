spring:  
  rabbitmq:
    host: localhost
    username: guest
    password: guest
    port: 5672
  boot:
    message: dev-profile-activated-again-2nd-times
  datasource:
    url: jdbc:postgresql://localhost:5432/department_db
    username: emp_user
    password: Pali$8619216837
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always


eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: "http://localhost:8761/eureka"
